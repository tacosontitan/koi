/**
 * Represents a response to a request within the context of the extension.
 */
export interface Response
{
    /**
     * The ID of the request the response is associated with.
     */
    id: string;
}